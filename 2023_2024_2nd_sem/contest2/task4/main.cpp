#include "iostream"




int main (){
    std::string s;
    std::cin >> s;
    char cancelled[8] = {'a', 'e', 'h', 'i', 'o', 'u', 'w', 'y'};
    for (char c : s) {
        if (cancelled.) {
            no_spaces += c;
        }
    }
    return 0;
}